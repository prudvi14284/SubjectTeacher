@model SubjectTeacher.Models.Subject

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Subject</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SubjectId" />
            <div class="form-group">
                <label asp-for="SunbjectName" class="control-label"></label>
                <input asp-for="SunbjectName" class="form-control" />
                <span asp-validation-for="SunbjectName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SubjectTerm" class="control-label"></label>
                <input asp-for="SubjectTerm" class="form-control" />
                <span asp-validation-for="SubjectTerm" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SubjectCredits" class="control-label"></label>
                <input asp-for="SubjectCredits" class="form-control" />
                <span asp-validation-for="SubjectCredits" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
